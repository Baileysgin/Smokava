# پنل ادمین Smokava

پنل مدیریت ادمین برای سیستم Smokava با استفاده از Vite، React، TypeScript و Ant Design.

## ویژگی‌ها

- ✅ لاگین امن با username/password
- 📊 داشبورد با آمار کامل سیستم
- 🏪 مدیریت کامل رستوران‌ها (CRUD)
- 📦 مشاهده لیست قلیون‌های مصرف شده
- 👥 مدیریت کاربران و نقش‌ها
- 📝 مدیریت پست‌ها و نظرات (Moderation)
- 🎨 رابط کاربری زیبا با Ant Design
- 🇮🇷 پشتیبانی کامل از فارسی و RTL

## نصب و راه‌اندازی روی سرور

### 1. تنظیم متغیرهای محیطی

در فایل `.env` در ریشه پروژه (یا در `docker-compose.yml`):

```env
VITE_API_URL=https://api.smokava.com/api
```

### 2. ایجاد ادمین اولیه

قبل از استفاده، باید یک کاربر ادمین ایجاد کنید:

```bash
# روی سرور
cd /opt/smokava
docker-compose exec backend node scripts/createAdmin.js username password
```

مثال:
```bash
docker-compose exec backend node scripts/createAdmin.js admin yoursecurepassword
```

### 3. دسترسی به پنل

پس از deployment، پنل ادمین در آدرس زیر در دسترس است:
- `https://admin.smokava.com` (یا آدرس سرور شما)

## ساختار پروژه

```
admin-panel/
├── src/
│   ├── components/      # کامپوننت‌های مشترک
│   │   ├── Layout.tsx   # Layout اصلی
│   │   └── ProtectedRoute.tsx
│   ├── pages/           # صفحات اصلی
│   │   ├── Login.tsx
│   │   ├── Dashboard.tsx
│   │   ├── Restaurants.tsx
│   │   ├── Users.tsx
│   │   ├── Moderation.tsx
│   │   └── PackageManagement.tsx
│   ├── services/        # سرویس‌های API
│   │   └── adminService.ts
│   ├── store/           # مدیریت state (Zustand)
│   │   └── authStore.ts
│   ├── lib/             # ابزارهای کمکی
│   │   └── api.ts       # تنظیمات Axios
│   └── App.tsx          # کامپوننت اصلی
```

## صفحات

### 1. لاگین (`/login`)
صفحه ورود به سیستم با username و password.

### 2. داشبورد (`/`)
نمایش آمار کلی سیستم:
- تعداد کل کاربران
- تعداد رستوران‌ها
- تعداد پست‌ها
- تعداد پکیج‌های فروخته شده
- تعداد قلیون‌های مصرف شده
- کاربران جدید در ۷ روز گذشته

### 3. مدیریت رستوران‌ها (`/restaurants`)
- مشاهده لیست همه رستوران‌ها
- افزودن رستوران جدید
- ویرایش رستوران
- حذف رستوران

### 4. مدیریت کاربران (`/users`)
- مشاهده لیست کاربران
- مشاهده جزئیات کاربر
- تخصیص نقش (User/Operator/Admin)
- تخصیص رستوران به Operator

### 5. مدیریت پست‌ها و نظرات (`/moderation`)
- مشاهده لیست پست‌ها
- مخفی/نمایش کردن پست‌ها
- حذف پست‌ها
- حذف نظرات

### 6. مدیریت پکیج (`/package-management`)
- مشاهده و مدیریت پکیج‌ها
- ایجاد پکیج جدید
- ویرایش پکیج
- فعال‌سازی پکیج برای کاربران

## API Endpoints

تمام API ها در `/api/admin/` قرار دارند:

- `POST /api/admin/login` - ورود ادمین
- `GET /api/admin/dashboard/stats` - دریافت آمار
- `GET /api/admin/restaurants` - لیست رستوران‌ها
- `POST /api/admin/restaurants` - ایجاد رستوران
- `PUT /api/admin/restaurants/:id` - ویرایش رستوران
- `DELETE /api/admin/restaurants/:id` - حذف رستوران
- `GET /api/admin/consumed-packages` - لیست قلیون‌های مصرف شده
- `GET /api/admin/users` - لیست کاربران
- `GET /api/admin/posts` - لیست پست‌ها برای moderation
- `POST /api/admin/users/:id/roles` - تخصیص نقش

## تکنولوژی‌ها

- **Vite** - Build tool و dev server
- **React 19** - کتابخانه UI
- **TypeScript** - زبان برنامه‌نویسی
- **Ant Design** - کتابخانه کامپوننت‌های UI
- **React Router** - مدیریت routing
- **Zustand** - مدیریت state
- **Axios** - HTTP client

## نکات مهم

1. پنل ادمین باید به backend متصل باشد (از طریق `VITE_API_URL`)
2. قبل از لاگین، حتماً یک کاربر ادمین ایجاد کنید
3. توکن احراز هویت در localStorage ذخیره می‌شود
4. در صورت انقضای توکن، به صورت خودکار به صفحه لاگین هدایت می‌شود
5. تمام URL ها باید از HTTPS استفاده کنند در production

## Deployment

پنل ادمین به صورت خودکار با `docker-compose` deploy می‌شود. برای اطلاعات بیشتر به `DOCS/DEPLOY.md` مراجعه کنید.
