# پنل ادمین Smokava

پنل مدیریت ادمین برای سیستم Smokava با استفاده از Vite، React، TypeScript و Ant Design.

## ویژگی‌ها

- ✅ لاگین امن با username/password
- 📊 داشبورد با آمار کامل سیستم
- 🏪 مدیریت کامل رستوران‌ها (CRUD)
- 📦 مشاهده لیست قلیون‌های مصرف شده
- 🎨 رابط کاربری زیبا با Ant Design
- 🇮🇷 پشتیبانی کامل از فارسی و RTL

## نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
cd admin-panel
npm install
```

### 2. تنظیم متغیرهای محیطی

یک فایل `.env` در پوشه `admin-panel` ایجاد کنید:

```env
VITE_API_URL=http://localhost:5000/api
```

### 3. ایجاد ادمین اولیه

قبل از استفاده، باید یک کاربر ادمین ایجاد کنید:

```bash
cd ../backend
node scripts/createAdmin.js username password
```

مثال:
```bash
node scripts/createAdmin.js admin admin123
```

### 4. اجرای پروژه

```bash
cd admin-panel
npm run dev
```

پنل ادمین روی `http://localhost:5173` اجرا می‌شود.

## ساختار پروژه

```
admin-panel/
├── src/
│   ├── components/      # کامپوننت‌های مشترک
│   │   ├── Layout.tsx   # Layout اصلی
│   │   └── ProtectedRoute.tsx
│   ├── pages/           # صفحات اصلی
│   │   ├── Login.tsx
│   │   ├── Dashboard.tsx
│   │   ├── Restaurants.tsx
│   │   └── ConsumedPackages.tsx
│   ├── services/        # سرویس‌های API
│   │   └── adminService.ts
│   ├── store/           # مدیریت state (Zustand)
│   │   └── authStore.ts
│   ├── lib/             # ابزارهای کمکی
│   │   └── api.ts       # تنظیمات Axios
│   └── App.tsx          # کامپوننت اصلی
```

## صفحات

### 1. لاگین (`/login`)
صفحه ورود به سیستم با username و password.

### 2. داشبورد (`/`)
نمایش آمار کلی سیستم:
- تعداد کل کاربران
- تعداد رستوران‌ها
- تعداد پست‌ها
- تعداد پکیج‌های فروخته شده
- تعداد قلیون‌های مصرف شده
- کاربران جدید در ۷ روز گذشته

### 3. مدیریت رستوران‌ها (`/restaurants`)
- مشاهده لیست همه رستوران‌ها
- افزودن رستوران جدید
- ویرایش رستوران
- حذف رستوران

### 4. قلیون‌های مصرف شده (`/consumed`)
مشاهده لیست کامل قلیون‌های مصرف شده با جزئیات:
- اطلاعات کاربر
- اطلاعات پکیج
- اطلاعات رستوران
- تاریخ و زمان مصرف
- طعم قلیون

## API Endpoints

تمام API ها در `/api/admin/` قرار دارند:

- `POST /api/admin/login` - ورود ادمین
- `GET /api/admin/dashboard/stats` - دریافت آمار
- `GET /api/admin/restaurants` - لیست رستوران‌ها
- `POST /api/admin/restaurants` - ایجاد رستوران
- `PUT /api/admin/restaurants/:id` - ویرایش رستوران
- `DELETE /api/admin/restaurants/:id` - حذف رستوران
- `GET /api/admin/consumed-packages` - لیست قلیون‌های مصرف شده
- `GET /api/admin/users` - لیست کاربران

## تکنولوژی‌ها

- **Vite** - Build tool و dev server
- **React 19** - کتابخانه UI
- **TypeScript** - زبان برنامه‌نویسی
- **Ant Design** - کتابخانه کامپوننت‌های UI
- **React Router** - مدیریت routing
- **Zustand** - مدیریت state
- **Axios** - HTTP client

## نکات مهم

1. برای استفاده از پنل، ابتدا باید backend را اجرا کنید.
2. قبل از لاگین، حتماً یک کاربر ادمین ایجاد کنید.
3. توکن احراز هویت در localStorage ذخیره می‌شود.
4. در صورت انقضای توکن، به صورت خودکار به صفحه لاگین هدایت می‌شوید.
